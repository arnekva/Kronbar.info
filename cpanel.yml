---
deployment:
  tasks:
    - export DEPLOYPATH=/home/eiviwbc/kronbar.arnekvaleberg.com
    - /bin/cp -R images $DEPLOYPATH
    - /bin/cp -R css $DEPLOYPATH
    - /bin/cp -R js $DEPLOYPATH
    - /bin/cp -R $DEPLOYPATH
